# Database connection
DATABASE_URL="postgresql://user:password@localhost:5432/catering_db?schema=public"

# Firebase config is loaded from fireconfig.json in project root
# Copy fireconfig.example.json to fireconfig.json and add your credentials

# Admin UIDs (comma-separated)
ADMIN_UIDS=

# Frontend origin (CORS)
FRONTEND_URL=http://localhost:3000

# Dev mode
NODE_ENV=development
PORT=3001
LOG_LEVEL=info
